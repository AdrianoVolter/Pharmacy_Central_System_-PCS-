const { resolve } = require("path"); // Importa o método resolve do módulo path

module.exports = {
  config: resolve(__dirname, "src", "config", "database.config.js"), // Caminho do arquivo de configuração do banco de dados

  "models-path": resolve(__dirname, "src", "models"), // Caminho da pasta onde ficarão os models

  "migrations-path": resolve(__dirname, "src", "database", "migrations"), // Caminho da pasta onde ficarão as migrations

  "seeders-path": resolve(__dirname, "src", "database", "seeders"), // Caminho da pasta onde ficarão os seeders
};
